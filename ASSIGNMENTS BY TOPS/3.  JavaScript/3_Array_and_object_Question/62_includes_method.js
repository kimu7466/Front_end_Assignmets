// Q.62 Check if the string contains a word Script using includes() method? 

const sentence = "JavaScript is a scripting language.";
const word = "Script";

if (sentence.includes(word)) {
    console.log(`The string "${sentence}" contains the word "${word}".`);
} else {
    console.log(`The string "${sentence}" does not contain the word "${word}".`);
}


